# Archived configs

Superseded stages, kept for reproducibility — each shipped generation's
weights are in `models/`. The live curriculum is the three configs in the
parent directory: `aim_smooth_v4` -> `stage7a_comboschool` -> `stage7b_chain`.
